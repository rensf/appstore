spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/app_store?serverTimezone=GMT%2B8
    username: root
    password: '263661'
  redis:
    host: 127.0.0.1
    port: 6379
    jedis:
      pool:
        max-active: 8
        max-wait: 1
        max-idle: 8
        min-idle: 0
    timeout: 5000
    database: 0
server:
  port: 8082

logging:
  level:
    com.sys.appstore: debug

project:
  filepath: ./file/
